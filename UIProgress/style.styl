// Skin-specific styles go here. Demo-specific styles go in demo/style.styl.

.ui-progress-wrapper {
    border: 1px solid borderColor-UIProgress;
    border-radius: borderRadius;
    height: 30px;
    overflow: hidden;
    position: relative;
}

.ui-progress {
    background: background-UIProgress;
    position: absolute;
    top: 0;
    left: 0;
    bottom: 0;
}

.ui-progress-label {
    color: color-UIProgress;
    line-height: 28px;
    padding-left: 10px;
    position: relative;
    text-align: left;
    z-index: 1;
}

@keyframes interdeterminate-progress-bar {
    from { opacity: 0; }
    to { opacity: 1; }
}

.ui-progress-indeterminate {
    background: background-UIProgress;
    border-radius: 0;
    right: 0;
    opacity: 0.25;
    animation: interdeterminate-progress-bar 2.5s ease-in-out alternate infinite;
}
