{
  "name": "enigma-uikit",
  "description": "Intuitive, accessible solutions for common UI needs in React.",
  "private": true,
  "version": "1.0.0-beta",
  "contributors": [
    "Evan Jacobs <evan.jacobs@enigma.io>"
  ],
  "main": "exports.js",
  "dependencies": {
    "classnames": "^2.1.5",
    "lodash.first": "^3.0.0",
    "lodash.isequal": "^3.0.4",
    "lodash.last": "^3.0.0",
    "lodash.without": "^3.2.1"
  },
  "devDependencies": {
    "babel-eslint": "^4.0.0",
    "babelify": "^6.0.0",
    "browserify": "^12.0.0",
    "budo": "^6.0.0",
    "chai": "^3.4.0",
    "es6-promise": "^3.0.2",
    "esdoc": "^0.4.3",
    "eslint-plugin-react": "^3.0.0",
    "eslintify": "*",
    "exposify": "^0.5.0",
    "jsdoc": "git+https://github.com/jsdoc3/jsdoc.git",
    "jsdoc-react": "^0.3.2",
    "karma": "^0.13.0",
    "karma-browserify": "^4.3.0",
    "karma-chai-sinon": "^0.1.5",
    "karma-chrome-launcher": "^0.2.0",
    "karma-mocha": "^0.2.0",
    "karma-mocha-reporter": "^1.1.0",
    "karma-phantomjs2-launcher": "^0.3.2",
    "karma-source-map-support": "^1.1.0",
    "karma-sourcemap-loader": "^0.3.5",
    "lodash": "^3.10.0",
    "minami": "^1.1.0",
    "mocha": "^2.3.3",
    "pleeease": "^4.0.2",
    "pleeease-cli": "git+https://github.com/yaycmyk/pleeease-cli.git#fix-ignore",
    "react": "^0.14.2",
    "react-dom": "^0.14.2",
    "sinon": "^1.17.2",
    "sinon-chai": "^2.8.0",
    "uglify-js": "^2.4.23",
    "watchify": "^3.2.3"
  },
  "peerDependencies": {
    "react": "^0.14.2",
    "react-dom": "^0.14.2"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "scripts": {
    "test": "karma start",
    "debug": "karma start karma.debug.conf.js",
    "docs": "mkdir -p docs/sdk && find . -type f -name '*.js' -not \\( -path './node_modules/*' -or -path './docs/*' -or -path './dist/*' -or -path './*/demo/*' \\) -not -name 'unit.*' -mindepth 2 | xargs jsdoc -d docs/sdk -c docs/jsdoc.json README.md --verbose",
    "demo_css": "pleeease-watch",
    "demo_js": "budo demo.js:dist/demo.js --colors --live --open --index=demo.html --iw --debug -- -t babelify",
    "demo": "sh parallelize.sh \"npm run demo_css\" \"npm run demo_js\"",
    "exclude_peer_deps": "echo $(node -p \"'-x ' + Object.keys(require('./package').peerDependencies).join(' -x ')\")",
    "browserify_config": "echo ./exports.js -r ./exports.js:enigma-uikit -t babelify",
    "start": "watchify $(npm run -s browserify_config) -t eslintify -o dist/uikit.js --iw --verbose --colors --debug",
    "build": "browserify $(npm run -s browserify_config) $(npm run -s exclude_peer_deps) -o dist/uikit.js --debug",
    "build_release": "NODE_ENV=production browserify $(npm run -s browserify_config) $(npm run -s exclude_peer_deps) | uglifyjs -mc drop_console > dist/uikit.min.js",
    "build_standalone": "NODE_ENV=production browserify $(npm run -s browserify_config) -t [ exposify --expose [ --react React --react-dom ReactDOM ] ] $(npm run -s exclude_peer_deps) --debug -o dist/uikit-standalone.js",
    "build_standalone_release": "NODE_ENV=production browserify $(npm run -s browserify_config) -t [ exposify --expose [ --react React --react-dom ReactDOM ] ] $(npm run -s exclude_peer_deps) | uglifyjs -mc drop_console > dist/uikit-standalone.min.js",
    "all": "npm run build && npm run build_release && npm run build_standalone && npm run build_standalone_release"
  }
}
