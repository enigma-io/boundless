// Skin-specific styles go here. Demo-specific styles go in demo/style.styl.

.b-checkbox-wrapper {
    display: flex;
}

.b-checkbox,
.b-checkbox-label {
    cursor: pointer;
}

.b-checkbox-label {
    line-height: 1.1;
    padding-left: 0.25em;
}

.b-checkbox {
    appearance: none;
    background: background-Checkbox;
    border: 1px solid borderColor-Checkbox;
    border-radius: borderRadius;
    box-shadow: none;
    flex-shrink: 0;
    margin: 0 3px 0;
    padding: 0;
    position: relative;
    height: 16px;
    width: 16px;

    transition: border-color 200ms ease;
}

.b-checkbox:hover {
    border-color: borderColor-Checkbox-hover;
}

.b-checkbox:focus {
    border-color: borderColor-Checkbox-focus;
}

.b-checkbox-checked {
    background: background-Checkbox-checked;
    border-color: borderColor-Checkbox-checked;
}

.b-checkbox-mixed {
    background: background-Checkbox-indeterminate;
}
